import "../styles/loading.css";

export function Loading() {

    return (
        // <div id="chargement" className={navigation.state === "loading" ? "loading" : ""}>
        <div id="chargement" className="loading_events">
        </div>
    )
}
